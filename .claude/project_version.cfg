VERSION_STRATEGY=semver
# MAJOR.MILESTONE.PATCH — see .tekhton/DESIGN_v1.md §12.
# Only pyproject.toml carries a hard-coded version string; src/sdi/__init__.py
# reads from package metadata via importlib.metadata so it never drifts.
VERSION_FILES=pyproject.toml:.project.version
CURRENT_VERSION=0.14.1
